# Copilot Quick Reference - Akura SafeStride

## Project: AI-Powered Injury Prevention for Runners
- Stack: HTML, Tailwind CDN, Vanilla JS, Supabase, Vercel
- Production: https://www.akura.in
- Supabase: https://yawxlwcniqfspcgefuro.supabase.co

## Supabase Setup (Always use this)
```javascript
const supabaseUrl = 'https://yawxlwcniqfspcgefuro.supabase.co';
const supabaseKey = 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6Inlhd3hsd2NuaXFmc3BjZ2VmdXJvIiwicm9sZSI6ImFub24iLCJpYXQiOjE3MzcwODIzNjcsImV4cCI6MjA1MjY1ODM2N30.TUoLNdN3xEhvVqPiR5sTIq8oeHhLm0wPxYe5PzB5qRo';
const supabase = window.supabase.createClient(supabaseUrl, supabaseKey);
```

## Tables
- profiles (id, email, full_name, role, assessment_completed)
- assessments (athlete_id, aifri_score, risk_level, assessment_data)
- activity_logs (athlete_id, activity_date, distance_km, rpe)
- workouts (athlete_id, workout_type, scheduled_date, status)
- training_plans (athlete_id, weekly_distance_goal, status)
- pillar_progress (athlete_id, week_number, mobility_score, stability_score, etc.)
- injury_reports (athlete_id, injury_type, body_part, severity, status)

## Colors
- Primary: #667eea (purple), Secondary: #764ba2
- Success: #4CAF50, Warning: #FF9500, Danger: #FF6B6B

## Common Patterns

### Auth Check
```javascript
const { data: { user } } = await supabase.auth.getUser();
if (!user) window.location.href = '/login.html';
```

### Query Pattern
```javascript
const { data, error } = await supabase
    .from('profiles')
    .select('*')
    .eq('id', user.id)
    .single();
```

### Toast
```javascript
function showToast(message, type = 'info') {
    const toast = document.createElement('div');
    toast.className = `toast fixed top-4 right-4 px-6 py-4 rounded-lg shadow-lg text-white z-50 ${
        type === 'success' ? 'bg-green-500' : 'bg-red-500'
    }`;
    toast.textContent = message;
    document.body.appendChild(toast);
    setTimeout(() => toast.remove(), 3000);
}
```

## Style Guidelines
- Mobile-first responsive design
- Semantic HTML5
- Tailwind utility classes
- async/await for all async operations
- Always handle errors with try-catch
- Use template literals
- Add console.log for debugging
