[phases.setup]
nixPkgs = ['python310']

[phases.install]
cmds = ['cd ai_agents', 'pip install -r requirements.txt']

[phases.build]
cmds = ['echo "Python FastAPI build complete"']

[start]
cmd = 'cd ai_agents && uvicorn main:app --host 0.0.0.0 --port $PORT'
